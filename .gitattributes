# Git LFS — binary art/asset tracking (kept for LFS-capable environments).
#
# NOTE: the current connections gateway does NOT implement the Git LFS API, so
# in this sandbox LFS is left *uninstalled* locally and binaries are committed
# as plain Git blobs (force-added, like the existing asset zips / bark WAVs).
# These attributes are retained so that in an LFS-capable clone/sandbox new
# binaries are routed through LFS automatically. Existing plain-blob files stay
# as-is (no history rewrite / git lfs migrate on shared `main`).

# Pixel art / sprites / atlases / UI
*.png       filter=lfs diff=lfs merge=lfs -text
*.gif       filter=lfs diff=lfs merge=lfs -text

# Animation / tilemap source the game (or Tiled) can consume
*.aseprite  filter=lfs diff=lfs merge=lfs -text
*.ase       filter=lfs diff=lfs merge=lfs -text
*.tmx       filter=lfs diff=lfs merge=lfs -text
*.tsx       filter=lfs diff=lfs merge=lfs -text

# Fonts
*.otf       filter=lfs diff=lfs merge=lfs -text
*.ttf       filter=lfs diff=lfs merge=lfs -text

# Audio (so newly added clips also use LFS; existing bark WAVs stay plain blobs)
*.wav       filter=lfs diff=lfs merge=lfs -text
*.ogg       filter=lfs diff=lfs merge=lfs -text
*.mp3       filter=lfs diff=lfs merge=lfs -text
